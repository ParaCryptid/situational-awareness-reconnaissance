
from src.collectors.twitter import collect_tweets

def test_collect_tweets():
    api_key = "test"
    api_secret = "test"
    access_token = "test"
    access_token_secret = "test"
    query = "test"
    tweets = collect_tweets(api_key, api_secret, access_token, access_token_secret, query)
    assert isinstance(tweets, list)
